CREATE OR REPLACE PACKAGE "DIETPLAN"."FOOD_DAO_TEST" AS 
    --%suite(Test the FOOD_DAO package)
    
    --%test(test that data is inserted)
    procedure TEST_CREATE_FOOD_METADATA;
    
    
 
 
END FOOD_DAO_TEST;
/
